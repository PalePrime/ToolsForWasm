# ToolsForWasm

Collection of tools for use in other WASM-based projects.

This is basically a collection of sub-repositories, containing tool sources, together with build scripts to compile them for use in web browser based projects.